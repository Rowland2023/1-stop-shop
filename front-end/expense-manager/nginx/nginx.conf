server {
    listen 10000;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # This line is vital for React/SPAs to handle routing
        try_files $uri $uri/ /index.html;
    }
    
    # ... your api proxies
}